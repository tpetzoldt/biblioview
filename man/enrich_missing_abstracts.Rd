% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_missing_abstracts.R
\name{enrich_missing_abstracts}
\alias{enrich_missing_abstracts}
\title{Enrich Missing Abstracts from External Metadata APIs}
\usage{
enrich_missing_abstracts(
  df,
  email_contact = Sys.getenv("POLITE_EMAIL"),
  providers = list(epmc = fetch_abstract_epmc, openalex = fetch_abstract_openalex,
    crossref = fetch_abstract_crossref)
)
}
\arguments{
\item{df}{A data frame generated by \code{fetch_all_zotero_data}.}

\item{email_contact}{Character. Valid email address for polite API pools.
Defaults to the environment variable \code{POLITE_EMAIL}.}

\item{providers}{Named list of provider fetcher functions executed in list order.
Defaults to Europe PMC -> OpenAlex -> Crossref.}
}
\value{
Data frame with updated \code{Abstract} column where matches were found.
}
\description{
Enrich Missing Abstracts from External Metadata APIs
}
\examples{
\dontrun{
# Default usage (Europe PMC -> OpenAlex -> Crossref)
df_enriched <- enrich_missing_abstracts(df = my_zotero_df)

# Custom provider order: OpenAlex primary, Europe PMC secondary
custom_order <- list(
  openalex = fetch_abstract_openalex,
  epmc     = fetch_abstract_epmc
)

df_enriched <- enrich_missing_abstracts(
  df = my_zotero_df,
  providers = custom_order
)
}

}
